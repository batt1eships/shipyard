{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "npx lerna run build",
    "postinstall": "git submodule update --init --recursive"
  },
  "workspaces": [
    "packages/*",
    "cli/*",
    "functions/*",
    "utils"
  ],
  "devDependencies": {
    "lerna": "^8.1.7"
  }
}
